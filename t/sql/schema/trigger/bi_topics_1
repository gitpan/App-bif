CREATE TRIGGER bi_topics_1
BEFORE INSERT ON topics
FOR EACH ROW
BEGIN
    SELECT debug(
        'TRIGGER bi_topics_1',
        NEW.id,
        NEW.uuid,
        NEW.first_update_id,
        NEW.kind
    );

END;